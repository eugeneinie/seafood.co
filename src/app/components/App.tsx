import { useState } from 'react'
import { useQuery } from 'react-query'
// Components
import Drawer from '@material-ui/core/Drawer'
import LinearProgress from '@material-ui/core/LinearProgress'
import Grid from '@material-ui/core/Grid'
import AddShoppingCartIcon from '@material-ui/icons/AddShoppingCart'
import Badge from '@material-ui/core/Badge'
// styles from App.styles.tsx

// npx create-react-app react-shopping-cart --template typescript // this is not necessary

// npm i @material-ui/core @material-ui/icons // install the @material-ui react library

// npm i react-query // 

// npm i styled-components @types/styled-components

// delete all the default imports 
// delete App.css, App.test.tsx, index.css, logo.svg, reportWebVitals.ts and setupTests.ts
// inside the index.tsx, delete all the imports of the deleted files above
// delete the text about analytics and "reportWebVitals()" 
// delete the React.StrictMode tags(if its there) 
// you can also delete "import React from 'react'"

const App = () => {
	return <div className='App'>Start</div>
}

export default App

